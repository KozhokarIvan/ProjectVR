import { Button } from "@nextui-org/button";

export interface EditButtonProps {
  redirectUrl: string;
}

export default function EditButton({ redirectUrl }: EditButtonProps) {
  return <Button></Button>;
}
